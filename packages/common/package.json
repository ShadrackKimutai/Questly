{
  "name": "@questly/common",
  "type": "module",
  "scripts": {
    "types": "tsc --noEmit",
    "format": "prettier --check . --log-level warn",
    "format:fix": "prettier --write . --log-level warn"
  },
  "dependencies": {
    "socket.io": "^4.8.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "prettier": "^3.9.1",
    "@types/node": "^26.0.0",
    "typescript": "^6.0.3"
  }
}
